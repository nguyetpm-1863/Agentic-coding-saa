export function HashtagChip({ text }: { text: string }) {
  return (
    <span className="font-[family-name:var(--font-montserrat)] text-sm font-medium tracking-[0.1px] leading-5 text-[#FFEA9E]">
      {text}
    </span>
  );
}
